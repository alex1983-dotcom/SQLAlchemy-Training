
---

## Шаг 0: Импорты и подготовка БД

```python
# Полностью чистое начало — все импорты явно
from sqlalchemy import create_engine, MetaData, Table, Column, Integer, String, insert, select

# In-memory SQLite (ничего не сохраняется на диск)
engine = create_engine("sqlite:///:memory:", echo=True)  # echo=True → видим реальный SQL
metadata_obj = MetaData()
```

---

## Шаг 1: Создаём таблицу `user_account`

```python
user_table = Table(
    "user_account",
    metadata_obj,
    Column("id", Integer, primary_key=True),
    Column("name", String(30), nullable=False),
    Column("fullname", String),
)

# Физически создаём таблицу в БД
metadata_obj.create_all(engine)
```

> 💡 В консоли увидишь:
> ```
> CREATE TABLE user_account (
>     id INTEGER NOT NULL, 
>     name VARCHAR(30) NOT NULL, 
>     fullname VARCHAR, 
>     PRIMARY KEY (id)
> )
> ```

---

## Шаг 2: Формируем INSERT-выражение

```python
# Вариант A: значения внутри .values()
stmt_a = insert(user_table).values(name="spongebob", fullname="Spongebob Squarepants")

# Вариант B: "шаблон" без значений — передадим при выполнении
stmt_b = insert(user_table)

print("stmt_a:", stmt_a)
print("stmt_b:", stmt_b)
```

> 🔍 Оба — объекты `Insert`, НЕ строки SQL. Разница:
> - `stmt_a` — уже содержит данные (`compiled.params` покажет их)
> - `stmt_b` — универсальный шаблон для многократного использования

---

## Шаг 3: Выполняем одиночную вставку (connection.execute)

```python
with engine.connect() as conn:
    # Выполняем stmt_a — данные уже внутри
    result = conn.execute(stmt_a)
    
    # 🔴 ВАЖНО: в SQLAlchemy 2.0 commit() обязателен для изменений!
    conn.commit()
    
    # Анализируем результат
    print("rowcount:", result.rowcount)              # → 1
    print("inserted_primary_key:", result.inserted_primary_key)  # → (1,)
```

> 💡 `inserted_primary_key` — кортеж, даже если первичный ключ один.  
> ⚠️ Если забыть `conn.commit()` — запись НЕ сохранится (особенно критично в продакшене).

---

## Шаг 4: Проверяем результат через SELECT

```python
with engine.connect() as conn:
    result = conn.execute(select(user_table))
    rows = result.fetchall()
    print(rows)
    # [(1, 'spongebob', 'Spongebob Squarepants')]
```

---

## Шаг 5: Множественная вставка (executemany / bulk insert)

```python
# Подготавливаем данные
data = [
    {"name": "sandy", "fullname": "Sandy Cheeks"},
    {"name": "patrick", "fullname": "Patrick Star"},
    {"name": "squidward", "fullname": "Squidward Tentacles"},
]

with engine.connect() as conn:
    # stmt_b — тот самый "шаблон" без значений из Шага 2
    result = conn.execute(stmt_b, data)  # ← передаём список словарей
    conn.commit()
    
    print("Вставлено строк:", result.rowcount)  # → 3
```

> 💡 Это **executemany** — один SQL-запрос с несколькими наборами параметров.  
> Эффективнее, чем 3 отдельных `execute()`.

---

## Шаг 6: Проверяем все записи

```python
with engine.connect() as conn:
    result = conn.execute(select(user_table).order_by(user_table.c.id))
    for row in result:
        print(f"id={row.id}, name={row.name}, fullname={row.fullname}")
```

Вывод:
```
id=1, name=spongebob, fullname=Spongebob Squarepants
id=2, name=sandy, fullname=Sandy Cheeks
id=3, name=patrick, fullname=Patrick Star
id=4, name=squidward, fullname=Squidward Tentacles
```

---

## Шаг 7: (Опционально) RETURNING clause — получаем данные сразу при вставке

> ⚠️ Требует SQLite ≥ 3.35 (2021) или PostgreSQL. Проверим поддержку:

```python
import sqlite3
print("Версия SQLite:", sqlite3.sqlite_version)  # Должно быть ≥ 3.35

# Если поддерживается — работаем:
if sqlite3.sqlite_version_info >= (3, 35, 0):
    stmt_ret = insert(user_table).values(
        name="gary", fullname="Gary the Snail"
    ).returning(user_table.c.id, user_table.c.name)
    
    with engine.connect() as conn:
        result = conn.execute(stmt_ret)
        conn.commit()
        
        # Получаем вставленные данные СРАЗУ
        for row in result:
            print("RETURNING:", row)  # → (5, 'gary')
else:
    print("⚠️ RETURNING не поддерживается этой версией SQLite")
```

---

## 📊 Сравнительная таблица подходов

| Подход | Синтаксис | Когда использовать | Особенности |
|--------|-----------|-------------------|-------------|
| **Одиночная вставка** | `conn.execute(stmt, {"name": "x"})` | 1 запись | Просто, понятно |
| **Bulk insert** | `conn.execute(stmt, [ {...}, {...} ])` | ≥2 записей | Эффективнее (executemany) |
| **RETURNING** | `insert(...).returning(col)` | Нужны данные сразу после INSERT | Требует поддержки СУБД (PostgreSQL, SQLite ≥3.35) |
| **ORM-стиль** | `session.add(obj); session.commit()` | Работа с объектами Python | Не рассматриваем здесь — это Core tutorial |

---

## ❌ Типичные ошибки (чего избегать)

```python
# ❌ Ошибка 1: забыли commit()
with engine.connect() as conn:
    conn.execute(stmt_a)
    # conn.commit() ← ЗАБЫЛИ!
# → запись НЕ сохранится!

# ❌ Ошибка 2: передаём список вместо словаря для одиночной вставки
conn.execute(stmt_b, [{"name": "error"}])  # ← executemany, но для одной записи — избыточно

# ✅ Правильно для одной записи:
conn.execute(stmt_b, {"name": "ok"})
```

---

## 🧪 Твоя очередь — мини-практикум

```python
# Задание 1: Вставь 2 новых пользователя БЕЗ использования .values() при создании stmt
# (т.е. передай данные только в execute())

# Задание 2: Получи последнюю вставленную запись через SELECT с сортировкой по id DESC

# Задание 3 (продвинутое): Попробуй использовать .returning(), если твоя SQLite поддерживает
```

Хочешь, чтобы я:
1. Показал решения этих заданий?
2. Перешёл к следующему разделу туториала (`insert().returning()` подробно)?
3. Сравнил подходы с Django ORM (`Model.objects.create()` vs `bulk_create()`)?