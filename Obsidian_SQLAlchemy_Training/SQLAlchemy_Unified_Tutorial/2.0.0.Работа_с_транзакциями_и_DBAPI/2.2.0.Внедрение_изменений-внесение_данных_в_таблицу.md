### **Что такое ORM и зачем он нужен?**

### 🚩 Проблема: *"Я не хочу писать сырой SQL"*

Представь, что ты работаешь с базой данных напрямую через SQL. Чтобы добавить новую звезду в астрономическую базу, тебе нужно написать что-то вроде:

```sql
INSERT INTO stars (name, temperature_k, mass_solar) VALUES ('Betelgeuse', 3500, 11.6);
```

А чтобы получить данные — выполнить:

```sql
SELECT * FROM stars WHERE temperature_k < 4000;
```

Это **работает**, но:

- Ты смешиваешь логику приложения и SQL-запросы.
- Тебе приходится вручную следить за соответствием типов Python ↔ SQL.
- Любое изменение структуры БД требует правки **всех** строк запросов.
- Трудно поддерживать читаемость, особенно при сложных запросах.
- Нарушается принцип ООП — у тебя нет объекта «звезда», только строки и кортежи.

### ✅ Решение: **Объекты вместо строк**

ORM (**Object-Relational Mapping**, «отображение объектов на отношения») — это инструмент, который:

- Позволяет описывать таблицы БД как **классы Python**.
- Позволяет работать с записями как с **объектами**.
- Автоматически генерирует SQL под капотом.
- Скрывает большую часть рутинной работы с соединениями, транзакциями и типами.

Пример на SQLAlchemy 2.0:

```python
# Модель — это класс
class Star(Base):
    __tablename__ = "stars"
    id: Mapped[int] = mapped_column(primary_key=True)
    name: Mapped[str] = mapped_column(String(100))
    temperature_k: Mapped[float]
    mass_solar: Mapped[float]

# Создание объекта — как в ООП
betelgeuse = Star(name="Betelgeuse", temperature_k=3500, mass_solar=11.6)

# Сохранение — без SQL!
session.add(betelgeuse)
session.commit()
```

Теперь твоя «звезда» — полноценный Python-объект. У неё есть атрибуты, её можно передавать в функции, сериализовать, наследовать и т.д.

---

### ⚠️ Ограничения ORM: *Когда лучше писать SQL вручную?*

ORM — не панацея. Вот случаи, когда **лучше писать сырой SQL**:

| Сценарий | Почему SQL лучше |
|---------|------------------|
| Сложные аналитические запросы (JOIN + GROUP BY + оконные функции) | ORM делает код громоздким и неочевидным |
| Массовая обработка данных (миллионы строк) | ORM имеет накладные расходы (overhead) |
| Использование специфичных возможностей СУБД (например, полнотекстовый поиск в PostgreSQL) | ORM может не поддерживать это напрямую |
| Оптимизация производительности до последнего миллисекунда | Ты теряешь контроль над точным SQL |

👉 **Вывод**: ORM — это инструмент для **доменной логики** (например, астрономические объекты), а не для всего подряд.

---

### 🧠 Важная аналогия (из твоих предпочтений):

> Представь, что ORM — это **панель управления автомобилем**:  
> ты не лазишь под капот и не кручёшь болты напрямую (не пишешь SQL),  
> а просто нажимаешь на педали и крутишь руль (работаешь с объектами).  
> Но если машина сломалась — **иногда всё же нужно открыть капот** (написать SQL вручную).

---

### 💡 Что дальше?

Теперь, когда ты понимаешь, **зачем нужен ORM**, в следующем пункте мы сравним **SQLAlchemy**, **Django ORM** и **Peewee**, чтобы объяснить, почему SQLAlchemy кажется «сложным», но при этом — **мощным и гибким**.

Хочешь перейти к **пункту 2: «SQLAlchemy vs Django ORM vs Peewee — ключевые различия»**?  
Или у тебя есть вопросы по текущему материалу?