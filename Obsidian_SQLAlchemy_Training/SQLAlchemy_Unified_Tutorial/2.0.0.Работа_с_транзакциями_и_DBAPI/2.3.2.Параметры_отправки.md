### 📌 Параметры отправки (Bound Parameters) в SQLAlchemy 2.0 — развёрнутый разбор

---

#### 🔑 Базовый принцип: почему параметры «привязаны»

Когда вы пишете запрос с `:имя`, вы создаёте **шаблон**. Значения подставляются не в строку SQL, а передаются отдельно драйверу БД (psycopg2, sqlite3 и др.) через протокол параметризованных запросов:

```python
stmt = text("SELECT * FROM users WHERE age > :min_age")
result = conn.execute(stmt, {"min_age": 18})
```

Что происходит «под капотом»:
1. SQLAlchemy отправляет СУБД: `"SELECT * FROM users WHERE age > ?"` (или `$1` в PostgreSQL)
2. Отдельно передаёт значение `18` как бинарный параметр
3. СУБД сама подставляет значение **без интерпретации как SQL-кода**

→ Это гарантирует защиту от инъекций на уровне протокола БД, а не на уровне экранирования строк.

---

#### ⚙️ Синтаксис и методы передачи параметров

| Способ | Синтаксис в запросе | Передача значений | Поддержка драйверами |
|--------|---------------------|-------------------|----------------------|
| **Именованные** | `:имя` | `{"имя": значение}` | ✅ Все драйверы |
| **Позиционные** | `?` | `(значение1, значение2)` | ⚠️ Только некоторые (SQLite) |
| **PostgreSQL-стиль** | `:имя` → `$1` | Автоматическая подстановка | ✅ psycopg2 преобразует |

```python
# ✅ Именованные (рекомендуется)
stmt = text("SELECT * FROM users WHERE name = :name AND age > :min_age")
conn.execute(stmt, {"name": "Алиса", "min_age": 18})

# ⚠️ Позиционные (менее читаемо, зависит от драйвера)
stmt = text("SELECT * FROM users WHERE name = ? AND age > ?")
conn.execute(stmt, ("Алиса", 18))
```

---

#### 🧪 Практические примеры: от простого к сложному

##### 1. Одиночный параметр
```python
# ❌ НЕБЕЗОПАСНО: конкатенация
user_id = "1 OR 1=1"
stmt = text(f"SELECT * FROM users WHERE id = {user_id}")  # → SQL-инъекция!

# ✅ Безопасно: параметризация
stmt = text("SELECT * FROM users WHERE id = :user_id")
result = conn.execute(stmt, {"user_id": user_id})  # → значение экранируется
```

##### 2. Несколько параметров
```python
stmt = text("""
    SELECT id, name, email 
    FROM users 
    WHERE age BETWEEN :min_age AND :max_age 
    AND status = :status
""")
params = {"min_age": 18, "max_age": 65, "status": "active"}
result = conn.execute(stmt, params)
```

##### 3. Множественная отправка (bulk insert/update)
```python
stmt = text("INSERT INTO logs (user_id, action, timestamp) VALUES (:user_id, :action, :ts)")

# Передаём список словарей — один вызов execute()
data = [
    {"user_id": 1, "action": "login", "ts": "2024-01-01 10:00:00"},
    {"user_id": 2, "action": "logout", "ts": "2024-01-01 10:05:00"},
    {"user_id": 1, "action": "edit_profile", "ts": "2024-01-01 10:10:00"}
]
conn.execute(stmt, data)  # ← эффективно: один сетевой вызов
```

##### 4. Смешение с `.bindparams()` (явная привязка типов)
```python
from sqlalchemy import Integer

stmt = text("SELECT * FROM users WHERE id = :user_id").bindparams(
    bindparam("user_id", value=42, type_=Integer)
)
# Полезно при работе с нестандартными типами (UUID, JSON и т.д.)
```

---

#### ⚖️ Сравнение с Django ORM

| Аспект | SQLAlchemy Core 2.0 | Django ORM |
|--------|---------------------|------------|
| **Уровень контроля** | Полный: вы пишете SQL, сами решаете про параметры | Минимальный: параметры скрыты внутри генератора запросов |
| **Синтаксис параметров** | Явный: `:имя` + словарь в `.execute()` | Неявный: `filter(name="...")` → ORM сам строит параметризованный запрос |
| **Гибкость** | Можно использовать любые выражения СУБД (например, `::jsonb`) | Ограничен возможностями QuerySet API |
| **Производительность** | Возможна оптимизация «под конкретную СУБД» | Абстракция может добавлять накладные расходы |
| **Пример** | `text("WHERE data @> :filter").bindparams(filter='{"key":"value"}')` | `Model.objects.filter(data__contains={"key": "value"})` |

```python
# SQLAlchemy: прямой доступ к возможностям СУБД
stmt = text("""
    SELECT * FROM products 
    WHERE properties @> :filter  -- PostgreSQL JSONB оператор
""")
conn.execute(stmt, {"filter": '{"category": "electronics"}'})

# Django: абстракция скрывает детали
Product.objects.filter(properties__contains={"category": "electronics"})
# → генерирует тот же запрос, но вы не контролируете синтаксис напрямую
```

---

#### ⚠️ Подводные камни и типичные ошибки

| ❌ Ошибка | ✅ Исправление | Почему важно |
|-----------|----------------|--------------|
| **Конкатенация значений в SQL**<br>`f"WHERE id = {user_id}"` | Использовать `:имя` + словарь | Конкатенация = уязвимость к инъекциям даже при «надёжных» источниках данных |
| **Передача параметров как строка**<br>`text(f"WHERE id = ':id'")` | Параметр без кавычек: `WHERE id = :id` | Кавычки превращают параметр в литерал — значение не подставится |
| **Забыть передать значение**<br>`execute(stmt)` без словаря | Всегда передавать словарь, даже если параметр один | `sqlalchemy.exc.StatementError: (sqlalchemy.exc.InvalidRequestError) A value is required for bind parameter 'id'` |
| **Смешение `:имя` и `%s`** | Использовать только `:имя` | `%s` — синтаксис Python string formatting или psycopg2 напрямую; в SQLAlchemy работает только `:имя` |
| **Передача списка как значение**<br>`{"ids": [1,2,3]}` в `WHERE id IN :ids` | Использовать `bindparam(..., expanding=True)` или динамически строить запрос | Обычный параметр не раскрывает списки — нужна специальная обработка |

```python
# ❌ Не работает: список не раскроется автоматически
stmt = text("SELECT * FROM users WHERE id IN :user_ids")
conn.execute(stmt, {"user_ids": [1, 2, 3]})  # → ошибка синтаксиса

# ✅ Способ 1: динамическое формирование (для небольших списков)
ids = [1, 2, 3]
placeholders = ", ".join(f":id_{i}" for i in range(len(ids)))
stmt = text(f"SELECT * FROM users WHERE id IN ({placeholders})")
params = {f"id_{i}": id_val for i, id_val in enumerate(ids)}
conn.execute(stmt, params)

# ✅ Способ 2: использовать expanding bindparam (SQLAlchemy 1.4+)
from sqlalchemy import bindparam
stmt = text("SELECT * FROM users WHERE id IN :user_ids").bindparams(
    bindparam("user_ids", expanding=True)
)
conn.execute(stmt, {"user_ids": [1, 2, 3]})
```

---

#### 💡 Итог: ключевые правила

1. **Никогда** не подставляйте значения через конкатенацию или f-строки в тело SQL.
2. Все значения — **только** через `:имя` + словарь в `.execute()`.
3. Для списков в `IN (...)` используйте `expanding=True` или динамические плейсхолдеры.
4. Параметризация работает на уровне протокола БД — это надёжнее ручного экранирования.
5. При работе с нестандартными типами (JSON, UUID) используйте `.bindparams(type_=...)` для явного указания типа.