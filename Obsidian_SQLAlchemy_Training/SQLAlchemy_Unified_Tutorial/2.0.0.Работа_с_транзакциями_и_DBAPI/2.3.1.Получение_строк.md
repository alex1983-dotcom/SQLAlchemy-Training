Отлично. Ниже — **полный, подробный, самостоятельный текст для отдельного Markdown-файла**, соответствующий **пункту 4** из **Части 0** твоего курса:

---

## 4. **Версии: зачем SQLAlchemy 2.0?**

### Введение

SQLAlchemy — одна из самых зрелых и мощных библиотек Python, но до 2023 года она имела **два параллельных, но несовместимых стиля написания кода**:  
- **SQLAlchemy Core** — для низкоуровневой работы с SQL-выражениями,  
- **SQLAlchemy ORM** — для высокоуровневой работы с объектами.

Эти два слоя использовали **разные синтаксисы**, **разные функции** и **разные способы построения запросов**. Это создавало когнитивную нагрузку: разработчик должен был помнить, **как строить запрос в Core** и **как — в ORM**, даже если логика была одинаковой.

SQLAlchemy 2.0 — это не просто «новая версия». Это **архитектурная перезагрузка**, направленная на:
- устранение дублирования,
- унификацию API,
- введение статической типизации,
- упрощение обучения и поддержки.

Для новых проектов **использование SQLAlchemy 2.0 — это не рекомендация, а обязательное условие**.

---

### Упрощённый синтаксис

До версии 2.0 ORM-запросы писались через метод `session.query()`:

```python
# SQLAlchemy 1.x (устаревший стиль)
stars = session.query(Star).filter(Star.temperature_k < 4000).all()
```

Этот стиль:
- не совпадал с Core (`select()`),
- не поддерживал композицию запросов,
- был избыточным (`.all()`, `.first()`, `.one()` — разные методы для одного и того же действия).

SQLAlchemy 2.0 **полностью отказывается от `session.query()`** в пользу единого интерфейса — **`select()`**, который работает **и в Core, и в ORM**:

```python
# SQLAlchemy 2.0 (правильный стиль)
from sqlalchemy import select

stmt = select(Star).where(Star.temperature_k < 4000)
result = session.execute(stmt).scalars().all()
```

Этот подход:
- **единообразен**: `select()`, `insert()`, `update()`, `delete()` — теперь универсальны,
- **составляем**: можно строить запрос по частям и переиспользовать,
- **читаем**: `where()`, `order_by()`, `limit()` — цепочка методов напоминает SQL.

Более того, **все операции теперь выполняются через `session.execute()`**, что устраняет путаницу между «запросом» и «выполнением».

---

### Единый стиль для Core и ORM

Раньше разработчик, изучающий ORM, не мог использовать знания Core — и наоборот. Теперь это **одна система**.

Сравнение:

| Операция | Core (1.x) | ORM (1.x) | **SQLAlchemy 2.0 (и Core, и ORM)** |
|--------|------------|----------|-----------------------------------|
| Выборка | `table.select().where(...)` | `session.query(Model).filter(...)` | `select(Model).where(...)` |
| Вставка | `table.insert().values(...)` | `session.add(obj); session.commit()` | `session.execute(insert(Model).values(...))` |
| Обновление | `table.update().where(...).values(...)` | `obj.attr = x; session.commit()` | `session.execute(update(Model).where(...).values(...))` |

Теперь, если ты знаешь, как писать на Core — ты **автоматически понимаешь ORM**, и наоборот. Это особенно ценно, когда тебе нужно **смешать ORM и Core** в одном запросе (например, выбрать объекты ORM, но с фильтром через сырое выражение Core).

Пример смешанного запроса:

```python
from sqlalchemy import select, func

# ORM-модель + Core-функция
stmt = select(Star.name, func.avg(Planet.mass_earth)).join(Planet).group_by(Star.id)
result = session.execute(stmt).fetchall()
```

Здесь `Star` — ORM-модель, а `func.avg()` — Core-выражение. В 2.0 они **естественно совмещаются**.

---

### Поддержка типизации (`Mapped`, `mapped_column`)

Одна из главных проблем SQLAlchemy 1.x — **отсутствие поддержки статической типизации**. Ты писал:

```python
# 1.x — неизвестно, есть ли у объекта атрибут `name`
star = session.get(Star, 1)
print(star.name)  # PyCharm/mypy не знает, существует ли `name`
```

SQLAlchemy 2.0 **полностью аннотируем**. Он использует два новых инструмента:

- `Mapped[T]` — аннотация для атрибута, который отображается на колонку,
- `mapped_column()` — функция для описания колонки с поддержкой типов.

Пример модели:

```python
from typing import Optional
from sqlalchemy import String, Float, Integer
from sqlalchemy.orm import DeclarativeBase, Mapped, mapped_column

class Base(DeclarativeBase):
    pass

class Star(Base):
    __tablename__ = "stars"

    id: Mapped[int] = mapped_column(primary_key=True)
    name: Mapped[str] = mapped_column(String(100))
    temperature_k: Mapped[float]
    mass_solar: Mapped[Optional[float]]  # может быть NULL
```

Теперь:
- PyCharm **знает**, что у `Star` есть `name: str`,
- mypy **проверит**, что `mass_solar` может быть `None`,
- если ты напишешь `star.naem` — **IDE сразу подсветит ошибку**.

Это критически важно для:
- крупных проектов,
- рефакторинга,
- обучения (ты видишь, что у объекта есть, без чтения исходников).

Более того, **тип `Optional[T]` автоматически превращается в `NULL` в SQL**, а `T` — в `NOT NULL`. Никаких ручных `nullable=True`.

---

### Будущее SQLAlchemy — 2.0+

SQLAlchemy 1.4 — **последняя версия 1.x**. Она находится в режиме **только исправления критических багов** (security fixes). Новые функции, улучшения производительности, документация — всё это разрабатывается **исключительно для 2.x**.

Официальная позиция разработчиков:
> «Новые проекты должны использовать SQLAlchemy 2.0. Старые проекты следует мигрировать».

Игнорировать 2.0 — значит:
- использовать устаревший синтаксис,
- терять поддержку типизации,
- сталкиваться с несовместимостями в будущем,
- читать документацию, которая постепенно будет фокусироваться только на 2.0.

Для твоего случая — **чистого ORM-приложения с астрономической моделью** — SQLAlchemy 2.0 не просто удобен, а **необходим**. Он даёт:
- чистый, логичный код,
- защиту от опечаток через типизацию,
- возможность расти от простых моделей к сложным запросам без смены парадигмы.

---

### Заключение

SQLAlchemy 2.0 — это **ответ на десятилетний опыт** использования библиотеки. Он не «сложнее» — он **последователен**. Он не «меняет всё» — он **устраняет противоречия**.

Если ты переходишь из Django ORM, то:
- тебе уже знакома идея моделей и объектов,
- но ты **хочешь больше контроля и гибкости**,
- и SQLAlchemy 2.0 даёт это **без магии, с типизацией и единым стилем**.

Поэтому весь этот курс **строится исключительно на SQLAlchemy 2.0+**, без упоминания устаревших паттернов. Ты учишь **современный ORM**, каким он задуман в 2026 году.

---

> 📌 **Практическое правило**:  
> Если в документации или Stack Overflow ты видишь `session.query()` — это **устаревший код**. Закрывай вкладку. Ищи примеры с `select()`.

---

Готов к **Части 1: Установка и первый запуск**.  
Скажи: **«часть 1»** — и я начну с создания проекта, виртуального окружения и первого скрипта `hello_star.py`.